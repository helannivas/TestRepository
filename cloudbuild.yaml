steps:
  - name: 'python:3.12-slim'
    id: test-azure-token
    entrypoint: bash
    args:
      - -c
      - |
        pip install google-auth requests -q
        python3 scripts/test_cloudbuild_azure_token.py

options:
  logging: CLOUD_LOGGING_ONLY
